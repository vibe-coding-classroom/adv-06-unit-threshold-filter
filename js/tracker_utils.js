/**
 * 數學：待填寫重心座標計算公式
 */

function calculateCentroid(points) {
    // TODO: 學生需實作重心降維累加
    // if (points.length === 0) return null; // 邊界防護
    // let sumX = 0, sumY = 0;
    // ...
    // return { x: sumX / points.length, y: sumY / points.length };
    return { x: 0, y: 0 };
}

module.exports = {
    calculateCentroid
};
